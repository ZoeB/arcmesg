# An example .fetchmailrc to use.
# Replace the username and password.
# Replace the mda with a link to the "incoming-messages" directory.

poll pop.1and1.co.uk
	protocol pop3
	username "username"
	password "password"
	fetchall
	mda ~/incoming-messages/save.sh
